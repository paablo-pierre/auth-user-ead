server:
  port: 8087

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/auth-usergit push -uf origin main
    username: postgres
    password: asdqwe123
  jpa:
    hibernate:
      ddl-auto: update
      dialect: org.hibernate.dialect.PostgreSQLDialect
      jdbc:
        lob.non-contextual-creation: true
    properties:
      hibernate:
        show_sql: true